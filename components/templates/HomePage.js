import React from 'react';
import Banner from '../modules/Banner';

const HomePage = () => {
    return (
        <div>
            <Banner />
        </div>
    );
};

export default HomePage;